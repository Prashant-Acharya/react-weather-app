### A React app that uses weather API from [https://openweathermap.org] API to give current and daily weather.

To run the code open your terminal in current directory and type
	`npm install`
	`npm start`